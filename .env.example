# Decisions - Environment Variables Template
# Copy this file to .env and fill in the values

# =============================================================================
# Frontend Environment Variables (apps/web)
# =============================================================================
# These variables are exposed to the browser (prefixed with VITE_)

# API URL for backend communication
VITE_API_URL=http://localhost:3001

# Supabase Configuration (public/client-side)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# =============================================================================
# Backend Environment Variables (apps/api)
# =============================================================================
# These variables are server-side only (never exposed to browser)

# Server Configuration
PORT=3001
NODE_ENV=development

# Supabase Configuration (server-side)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# =============================================================================
# Notes
# =============================================================================
#
# VITE_SUPABASE_ANON_KEY: This is the public anonymous key from Supabase.
#   It's safe to expose in the browser as it has limited permissions.
#
# SUPABASE_SERVICE_ROLE_KEY: This is the secret service role key from Supabase.
#   NEVER expose this to the browser. It bypasses Row Level Security.
#
# To get your Supabase keys:
#   1. Go to your Supabase project dashboard
#   2. Navigate to Settings > API
#   3. Copy the URL, anon key, and service_role key
