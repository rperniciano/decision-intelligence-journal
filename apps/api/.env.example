# Decisions API - Environment Variables Template
# Copy this file to .env and fill in the values

# =============================================================================
# Server Configuration
# =============================================================================

# Port for the API server (default: 3001)
PORT=3001

# Node environment: development, production, test
NODE_ENV=development

# Host to bind to (default: 0.0.0.0 for Docker compatibility)
HOST=0.0.0.0

# Log level: trace, debug, info, warn, error, fatal
LOG_LEVEL=info

# CORS origin (set to specific origin in production, e.g., https://yourdomain.com)
CORS_ORIGIN=http://localhost:5173

# =============================================================================
# Supabase Configuration (REQUIRED)
# =============================================================================
# Get these from your Supabase project: Settings > API

# Supabase Project URL
SUPABASE_URL=https://your-project.supabase.co

# Supabase Anonymous Key (public key, safe for client-side)
SUPABASE_ANON_KEY=your-supabase-anon-key

# Supabase Service Role Key (SECRET - never expose to client!)
# This key bypasses Row Level Security - use only on server
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# =============================================================================
# AI Services (Optional - features work without these)
# =============================================================================

# AssemblyAI API Key for transcription
# Get from: https://www.assemblyai.com/dashboard/
ASSEMBLYAI_API_KEY=your-assemblyai-api-key

# OpenAI API Key for decision extraction
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key

# =============================================================================
# Notes
# =============================================================================
#
# SUPABASE_ANON_KEY: Public key with limited permissions. Safe to use on client.
#   RLS policies protect data even if exposed.
#
# SUPABASE_SERVICE_ROLE_KEY: Secret key that bypasses RLS!
#   NEVER expose to browser. Only use on server for admin operations.
#
# ASSEMBLYAI_API_KEY & OPENAI_API_KEY: Optional for MVP.
#   Without these, transcription and extraction will use mock services.
